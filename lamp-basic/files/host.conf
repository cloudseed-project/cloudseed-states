<VirtualHost {{ pillar.get('lampbasic:host', '*') }}:{{ pillar.get('lampbasic:port', 80) }}>
    ServerAdmin {{ pillar.get('lampbasic:server_admin', 'example@example.com') }}
    ServerName {{ pillar.get('lampbasic:server_name', 'example.com') }}
    ServerAlias {{ pillar.get('lampbasic:server_alias', 'www.example.com') }}

    DocumentRoot {{ pillar.get('lampbasic:document_root', '/var/www') }}
    <Directory />
            Options FollowSymLinks
            AllowOverride None
    </Directory>

    <Directory {{ pillar.get('lampbasic:document_root', '/var/www') }}>
            Options {{ pillar.get('lampbasic:options', ['Indexes', 'FollowSymLinks'])|join(' ') }}
            AllowOverride {{ pillar.get('lampbasic:allow_override', 'None') }}
            Order allow,deny
            allow from all
    </Directory>

    ErrorLog ${APACHE_LOG_DIR}/error.log

    # Possible values include: debug, info, notice, warn, error, crit,
    # alert, emerg.
    LogLevel warn

    CustomLog ${APACHE_LOG_DIR}/access.log combined
    # {{ pillar['lampbasic']['server_admin'] }}
</VirtualHost>
